1. Antes de começar a construir um site,
uma das técnicas é desenhar a sua
estrutura em forma de grades, mais
conhecida como:

    Resposta: wireframe.

2. Para que um site seja corretamente
exibido em vários tipos de dispositivos
com telas de tamanhos diferentes,
devemos nos preocupar bastante em
torná-lo:

    Resposta: responsivo.

3. Para que a largura de uma caixa
torne-se adaptável a vários tamanhos de
tela, utilizaremos as propriedades:

    Resposta: min-width e max-width.

4. Para declarar variáveis em CSS,
devemos criar uma pseudo-classe
especial dentro do documento CSS atual.
Qual é o nome dessa pseudo-classe?

    Resposta: :root.

5. Toda variável declarada em um
documento CSS deve obrigatoriamente
começar com:

    Resposta: dois traços --.

6. Um documento CSS que possui
variáveis definidas pode fazer referência
a elas utilizando a função:

    Resposta: var().

7. Os navegadores (user agents) aplicam
configurações padrão para todo e
qualquer elemento HTML5 que é exibido
como uma caixa . Para alterar as
definições dessas configurações default,
usamos um seletor CSS especial que é o:

    Resposta: asterisco * {}.

8. Para aplicar sombras às letras que
estão dentro de uma caixa, podemos
usar a propriedade:

    Resposta: text-shadow.

9. Ao usar listas do tipo <ul> em
HTML5, só conseguimos definir
marcadores do tipo circle, square ou
disc. Porém, podemos personalizar o
símbolo de cada elemento via CSS,
usando a propriedade:

    Resposta: list-style-type.

10. Qual dos valores a seguir é o único
que não será aceito para configurar a
propriedade position em CSS?

    Resposta: fullsize.